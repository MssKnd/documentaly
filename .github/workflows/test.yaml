name: Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Change Name
        uses: mssknd/documentary@main # アクションのリポジトリ
        with:
          target_branch: origin/main