{
  "tasks": {
    "check": "deno run --allow-env --allow-net --allow-read --allow-run main.ts check",
    "compile": "deno compile --unstable --allow-env --allow-net --allow-read --allow-run main.ts"
  }
}
